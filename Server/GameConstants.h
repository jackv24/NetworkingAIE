#pragma once

const float GAME_WIDTH = 50.0f;
const float GAME_HEIGHT = 30.0f;

const float STAGE_HEIGHT = 25.0f;

const float PADDLE_DISTANCE = 45.0f;
const float PADDLE_HEIGHT = 5.0f;
const float PADDLE_WIDTH = 1.0f;

const float BALL_RADIUS = 1.0f;